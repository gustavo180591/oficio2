{% extends 'base.html.twig' %}

{% block title %}REGISTRO DE OFICIOS{% endblock %}

{% block body %}
<div class="container">
    <div class="row justify-content-center">
        <div class="col-lg-10">
            <div class="card shadow-sm mb-5">
                <div class="card-body p-5 text-center">
                    <div class="text-center mb-4">
                        <img src="{{ asset('images/logo.png') }}" alt="Logo" class="img-fluid" style="max-width: 75%;">
                    </div>
                    <h1 class="text-3xl md:text-4xl font-bold text-orange-500 mb-4">BIENVENIDO AL REGISTRO DE OFICIOS DE LA CIUDAD DE POSADAS</h1>
                    <p class="text-lg text-gray-700 mb-6"">ENCUENTRA Y CONECTA DE MANERA FÁCIL Y RÁPIDA CON TRABAJADORES DE DIFERENTES OFICIOS</p>
                    
                    <div class="row mt-5 justify-content-center">
                        <div class="col-md-4 mb-4">
                            <div class="card h-100 border-0">
                                <div class="card-body">
                                    <div class="icon-box bg-primary bg-opacity-10 text-primary rounded-circle mx-auto mb-4" style="width: 80px; height: 80px; display: flex; align-items: center; justify-content: center;">
                                        <i class="fas fa-file-alt fa-2x"></i>
                                    </div>
                                    <h3>Buscador de Oficios</h3>
                                    <p class="text-muted">Los trabajadores de tu barrio, listos para hacer el trabajo que vos necesitás.</p>
                                    <a href="{{ path('app_oficio') }}" class="btn btn-outline-primary">Buscador</a>
                                </div>
                            </div>
                        </div>
                        
                        <div class="col-md-4 mb-4">
                            <div class="card h-100 border-0">
                                <div class="card-body">
                                    <div class="icon-box bg-success bg-opacity-10 text-success rounded-circle mx-auto mb-4" style="width: 80px; height: 80px; display: flex; align-items: center; justify-content: center;">
                                        <i class="fas fa-tasks fa-2x"></i>
                                    </div>
                                    <h3>Registros</h3>
                                    <p class="text-muted">Mantén un registro detallado de todas las actividades y seguimientos.</p>
                                    <a href="{{ path('app_registro') }}" class="btn btn-outline-success">Ver Registros</a>
                                </div>
                            </div>
                        </div>
                        
                        {% if is_granted('ROLE_ADMIN') %}
                        <div class="col-md-4 mb-4">
                            <div class="card h-100 border-0">
                                <div class="card-body">
                                    <div class="icon-box bg-info bg-opacity-10 text-info rounded-circle mx-auto mb-4" style="width: 80px; height: 80px; display: flex; align-items: center; justify-content: center;">
                                        <i class="fas fa-chart-line fa-2x"></i>
                                    </div>
                                    <h3>Reportes</h3>
                                    <p class="text-muted">Genera reportes detallados y análisis de la información.</p>
                                    <a href="{{ path('app_reportes') }}" class="btn btn-outline-info">Ver Reportes</a>
                                </div>
                            </div>
                        </div>
                        {% else %}
                        <!-- Empty column to maintain centering when there are only 2 columns -->
                        <div class="col-md-4"></div>
                        {% endif %}
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .icon-box {
        transition: all 0.3s ease;
    }
    
    .card:hover .icon-box {
        transform: scale(1.1);
    }
    
    .card {
        transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
    
    .card:hover {
        box-shadow: 0 10px 20px rgba(0,0,0,0.1) !important;
    }
    
    @media (max-width: 768px) {
        .display-4 {
            font-size: 2rem;
        }
        
        .lead {
            font-size: 1.1rem;
        }
    }
</style>
{% endblock %}
